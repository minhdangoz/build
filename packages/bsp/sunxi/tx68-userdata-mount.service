[Unit]
Description=Format (if needed) and mount the TX68 vendor userdata partition
DefaultDependencies=no
Before=local-fs.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/lib/armbian/tx68-userdata-mount.sh

[Install]
WantedBy=local-fs.target
